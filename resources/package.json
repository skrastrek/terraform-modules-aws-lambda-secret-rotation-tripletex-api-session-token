{
  "name": "lambda-secret-rotation-tripletex-api-session-token",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "axios": "^1.8.4",
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.148",
    "@types/node": "^22.13.9",
    "@aws-sdk/client-secrets-manager": "^3.777.0",
    "esbuild": "^0.25.2"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node20 --outdir=dist"
  }
}
